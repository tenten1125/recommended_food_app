<%= form_with model: @food, local: true do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name, required: true %>
  <%= form.label :comment %>
  <%= form.text_area :comment %>
  <%= form.submit "送信" %>
<% end %>